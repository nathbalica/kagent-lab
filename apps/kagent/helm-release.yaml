apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kagent-chart
  namespace: argocd

spec:
  project: default

  source:
    repoURL: oci://ghcr.io/kagent-dev/charts
    chart: kagent
    targetRevision: 0.1.0

  destination:
    server: https://kubernetes.default.svc
    namespace: kagent

  syncPolicy:
    automated:
      prune: true
      selfHeal: true